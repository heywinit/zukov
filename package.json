{
  "name": "zukov",
  "version": "0.1.0",
  "description": "A distributed compute framework for Bun with BEAM-inspired distributed task execution",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "keywords": [
    "distributed",
    "compute",
    "bun",
    "beam",
    "erlang"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "bun": ">=1.0.0"
  }
}
